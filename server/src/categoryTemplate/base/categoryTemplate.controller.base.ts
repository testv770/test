/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { CategoryTemplateService } from "../categoryTemplate.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CategoryTemplateCreateInput } from "./CategoryTemplateCreateInput";
import { CategoryTemplateWhereInput } from "./CategoryTemplateWhereInput";
import { CategoryTemplateWhereUniqueInput } from "./CategoryTemplateWhereUniqueInput";
import { CategoryTemplateFindManyArgs } from "./CategoryTemplateFindManyArgs";
import { CategoryTemplateUpdateInput } from "./CategoryTemplateUpdateInput";
import { CategoryTemplate } from "./CategoryTemplate";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class CategoryTemplateControllerBase {
  constructor(
    protected readonly service: CategoryTemplateService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "CategoryTemplate",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: CategoryTemplate })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: CategoryTemplateCreateInput
  ): Promise<CategoryTemplate> {
    return await this.service.create({
      data: {
        ...data,

        categoryId: data.categoryId
          ? {
              connect: data.categoryId,
            }
          : undefined,

        templateId: data.templateId
          ? {
              connect: data.templateId,
            }
          : undefined,
      },
      select: {
        categoryId: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,

        templateId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "CategoryTemplate",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [CategoryTemplate] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(CategoryTemplateFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<CategoryTemplate[]> {
    const args = plainToClass(CategoryTemplateFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        categoryId: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,

        templateId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "CategoryTemplate",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: CategoryTemplate })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: CategoryTemplateWhereUniqueInput
  ): Promise<CategoryTemplate | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        categoryId: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,

        templateId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "CategoryTemplate",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: CategoryTemplate })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: CategoryTemplateWhereUniqueInput,
    @common.Body() data: CategoryTemplateUpdateInput
  ): Promise<CategoryTemplate | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          categoryId: data.categoryId
            ? {
                connect: data.categoryId,
              }
            : undefined,

          templateId: data.templateId
            ? {
                connect: data.templateId,
              }
            : undefined,
        },
        select: {
          categoryId: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          id: true,

          templateId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "CategoryTemplate",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: CategoryTemplate })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: CategoryTemplateWhereUniqueInput
  ): Promise<CategoryTemplate | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          categoryId: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          id: true,

          templateId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
