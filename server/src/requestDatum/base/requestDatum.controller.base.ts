/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { RequestDatumService } from "../requestDatum.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { RequestDatumCreateInput } from "./RequestDatumCreateInput";
import { RequestDatumWhereInput } from "./RequestDatumWhereInput";
import { RequestDatumWhereUniqueInput } from "./RequestDatumWhereUniqueInput";
import { RequestDatumFindManyArgs } from "./RequestDatumFindManyArgs";
import { RequestDatumUpdateInput } from "./RequestDatumUpdateInput";
import { RequestDatum } from "./RequestDatum";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class RequestDatumControllerBase {
  constructor(
    protected readonly service: RequestDatumService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "RequestDatum",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: RequestDatum })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: RequestDatumCreateInput
  ): Promise<RequestDatum> {
    return await this.service.create({
      data: {
        ...data,

        askId: data.askId
          ? {
              connect: data.askId,
            }
          : undefined,

        categoryRequestId: data.categoryRequestId
          ? {
              connect: data.categoryRequestId,
            }
          : undefined,

        fileId: data.fileId
          ? {
              connect: data.fileId,
            }
          : undefined,

        linkId: data.linkId
          ? {
              connect: data.linkId,
            }
          : undefined,

        recipientId: data.recipientId
          ? {
              connect: data.recipientId,
            }
          : undefined,

        statusId: data.statusId
          ? {
              connect: data.statusId,
            }
          : undefined,
      },
      select: {
        askId: {
          select: {
            id: true,
          },
        },

        categoryRequestId: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        duration: true,

        fileId: {
          select: {
            id: true,
          },
        },

        id: true,

        linkId: {
          select: {
            id: true,
          },
        },

        recipientId: {
          select: {
            id: true,
          },
        },

        statusId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "RequestDatum",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [RequestDatum] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(RequestDatumFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<RequestDatum[]> {
    const args = plainToClass(RequestDatumFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        askId: {
          select: {
            id: true,
          },
        },

        categoryRequestId: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        duration: true,

        fileId: {
          select: {
            id: true,
          },
        },

        id: true,

        linkId: {
          select: {
            id: true,
          },
        },

        recipientId: {
          select: {
            id: true,
          },
        },

        statusId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "RequestDatum",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: RequestDatum })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: RequestDatumWhereUniqueInput
  ): Promise<RequestDatum | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        askId: {
          select: {
            id: true,
          },
        },

        categoryRequestId: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        duration: true,

        fileId: {
          select: {
            id: true,
          },
        },

        id: true,

        linkId: {
          select: {
            id: true,
          },
        },

        recipientId: {
          select: {
            id: true,
          },
        },

        statusId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "RequestDatum",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: RequestDatum })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: RequestDatumWhereUniqueInput,
    @common.Body() data: RequestDatumUpdateInput
  ): Promise<RequestDatum | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          askId: data.askId
            ? {
                connect: data.askId,
              }
            : undefined,

          categoryRequestId: data.categoryRequestId
            ? {
                connect: data.categoryRequestId,
              }
            : undefined,

          fileId: data.fileId
            ? {
                connect: data.fileId,
              }
            : undefined,

          linkId: data.linkId
            ? {
                connect: data.linkId,
              }
            : undefined,

          recipientId: data.recipientId
            ? {
                connect: data.recipientId,
              }
            : undefined,

          statusId: data.statusId
            ? {
                connect: data.statusId,
              }
            : undefined,
        },
        select: {
          askId: {
            select: {
              id: true,
            },
          },

          categoryRequestId: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          duration: true,

          fileId: {
            select: {
              id: true,
            },
          },

          id: true,

          linkId: {
            select: {
              id: true,
            },
          },

          recipientId: {
            select: {
              id: true,
            },
          },

          statusId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "RequestDatum",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: RequestDatum })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: RequestDatumWhereUniqueInput
  ): Promise<RequestDatum | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          askId: {
            select: {
              id: true,
            },
          },

          categoryRequestId: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          duration: true,

          fileId: {
            select: {
              id: true,
            },
          },

          id: true,

          linkId: {
            select: {
              id: true,
            },
          },

          recipientId: {
            select: {
              id: true,
            },
          },

          statusId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
