/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { EnumSuggestionDatumService } from "../enumSuggestionDatum.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { EnumSuggestionDatumCreateInput } from "./EnumSuggestionDatumCreateInput";
import { EnumSuggestionDatumWhereInput } from "./EnumSuggestionDatumWhereInput";
import { EnumSuggestionDatumWhereUniqueInput } from "./EnumSuggestionDatumWhereUniqueInput";
import { EnumSuggestionDatumFindManyArgs } from "./EnumSuggestionDatumFindManyArgs";
import { EnumSuggestionDatumUpdateInput } from "./EnumSuggestionDatumUpdateInput";
import { EnumSuggestionDatum } from "./EnumSuggestionDatum";
import { SuggestionDatumFindManyArgs } from "../../suggestionDatum/base/SuggestionDatumFindManyArgs";
import { SuggestionDatum } from "../../suggestionDatum/base/SuggestionDatum";
import { SuggestionDatumWhereUniqueInput } from "../../suggestionDatum/base/SuggestionDatumWhereUniqueInput";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class EnumSuggestionDatumControllerBase {
  constructor(
    protected readonly service: EnumSuggestionDatumService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "EnumSuggestionDatum",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: EnumSuggestionDatum })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: EnumSuggestionDatumCreateInput
  ): Promise<EnumSuggestionDatum> {
    return await this.service.create({
      data: data,
      select: {
        createdAt: true,
        id: true,
        status: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "EnumSuggestionDatum",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [EnumSuggestionDatum] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(EnumSuggestionDatumFindManyArgs)
  async findMany(
    @common.Req() request: Request
  ): Promise<EnumSuggestionDatum[]> {
    const args = plainToClass(EnumSuggestionDatumFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        createdAt: true,
        id: true,
        status: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "EnumSuggestionDatum",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: EnumSuggestionDatum })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: EnumSuggestionDatumWhereUniqueInput
  ): Promise<EnumSuggestionDatum | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,
        id: true,
        status: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "EnumSuggestionDatum",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: EnumSuggestionDatum })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: EnumSuggestionDatumWhereUniqueInput,
    @common.Body() data: EnumSuggestionDatumUpdateInput
  ): Promise<EnumSuggestionDatum | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          createdAt: true,
          id: true,
          status: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "EnumSuggestionDatum",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: EnumSuggestionDatum })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: EnumSuggestionDatumWhereUniqueInput
  ): Promise<EnumSuggestionDatum | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,
          id: true,
          status: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "SuggestionDatum",
    action: "read",
    possession: "any",
  })
  @common.Get("/:id/suggestionData")
  @ApiNestedQuery(SuggestionDatumFindManyArgs)
  async findManySuggestionData(
    @common.Req() request: Request,
    @common.Param() params: EnumSuggestionDatumWhereUniqueInput
  ): Promise<SuggestionDatum[]> {
    const query = plainToClass(SuggestionDatumFindManyArgs, request.query);
    const results = await this.service.findSuggestionData(params.id, {
      ...query,
      select: {
        categorySuggestionId: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        duration: true,
        id: true,

        recipientId: {
          select: {
            id: true,
          },
        },

        statusId: {
          select: {
            id: true,
          },
        },

        suggestionId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @nestAccessControl.UseRoles({
    resource: "EnumSuggestionDatum",
    action: "update",
    possession: "any",
  })
  @common.Post("/:id/suggestionData")
  async connectSuggestionData(
    @common.Param() params: EnumSuggestionDatumWhereUniqueInput,
    @common.Body() body: SuggestionDatumWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      suggestionData: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @nestAccessControl.UseRoles({
    resource: "EnumSuggestionDatum",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id/suggestionData")
  async updateSuggestionData(
    @common.Param() params: EnumSuggestionDatumWhereUniqueInput,
    @common.Body() body: SuggestionDatumWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      suggestionData: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @nestAccessControl.UseRoles({
    resource: "EnumSuggestionDatum",
    action: "update",
    possession: "any",
  })
  @common.Delete("/:id/suggestionData")
  async disconnectSuggestionData(
    @common.Param() params: EnumSuggestionDatumWhereUniqueInput,
    @common.Body() body: SuggestionDatumWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      suggestionData: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
