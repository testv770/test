/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { EnumRequestDatumService } from "../enumRequestDatum.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { EnumRequestDatumCreateInput } from "./EnumRequestDatumCreateInput";
import { EnumRequestDatumWhereInput } from "./EnumRequestDatumWhereInput";
import { EnumRequestDatumWhereUniqueInput } from "./EnumRequestDatumWhereUniqueInput";
import { EnumRequestDatumFindManyArgs } from "./EnumRequestDatumFindManyArgs";
import { EnumRequestDatumUpdateInput } from "./EnumRequestDatumUpdateInput";
import { EnumRequestDatum } from "./EnumRequestDatum";
import { RequestDatumFindManyArgs } from "../../requestDatum/base/RequestDatumFindManyArgs";
import { RequestDatum } from "../../requestDatum/base/RequestDatum";
import { RequestDatumWhereUniqueInput } from "../../requestDatum/base/RequestDatumWhereUniqueInput";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class EnumRequestDatumControllerBase {
  constructor(
    protected readonly service: EnumRequestDatumService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "EnumRequestDatum",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: EnumRequestDatum })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: EnumRequestDatumCreateInput
  ): Promise<EnumRequestDatum> {
    return await this.service.create({
      data: data,
      select: {
        createdAt: true,
        id: true,
        status: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "EnumRequestDatum",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [EnumRequestDatum] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(EnumRequestDatumFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<EnumRequestDatum[]> {
    const args = plainToClass(EnumRequestDatumFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        createdAt: true,
        id: true,
        status: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "EnumRequestDatum",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: EnumRequestDatum })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: EnumRequestDatumWhereUniqueInput
  ): Promise<EnumRequestDatum | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,
        id: true,
        status: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "EnumRequestDatum",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: EnumRequestDatum })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: EnumRequestDatumWhereUniqueInput,
    @common.Body() data: EnumRequestDatumUpdateInput
  ): Promise<EnumRequestDatum | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          createdAt: true,
          id: true,
          status: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "EnumRequestDatum",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: EnumRequestDatum })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: EnumRequestDatumWhereUniqueInput
  ): Promise<EnumRequestDatum | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,
          id: true,
          status: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "RequestDatum",
    action: "read",
    possession: "any",
  })
  @common.Get("/:id/requestData")
  @ApiNestedQuery(RequestDatumFindManyArgs)
  async findManyRequestData(
    @common.Req() request: Request,
    @common.Param() params: EnumRequestDatumWhereUniqueInput
  ): Promise<RequestDatum[]> {
    const query = plainToClass(RequestDatumFindManyArgs, request.query);
    const results = await this.service.findRequestData(params.id, {
      ...query,
      select: {
        askId: {
          select: {
            id: true,
          },
        },

        categoryRequestId: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        duration: true,

        fileId: {
          select: {
            id: true,
          },
        },

        id: true,

        linkId: {
          select: {
            id: true,
          },
        },

        recipientId: {
          select: {
            id: true,
          },
        },

        statusId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @nestAccessControl.UseRoles({
    resource: "EnumRequestDatum",
    action: "update",
    possession: "any",
  })
  @common.Post("/:id/requestData")
  async connectRequestData(
    @common.Param() params: EnumRequestDatumWhereUniqueInput,
    @common.Body() body: RequestDatumWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      requestData: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @nestAccessControl.UseRoles({
    resource: "EnumRequestDatum",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id/requestData")
  async updateRequestData(
    @common.Param() params: EnumRequestDatumWhereUniqueInput,
    @common.Body() body: RequestDatumWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      requestData: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @nestAccessControl.UseRoles({
    resource: "EnumRequestDatum",
    action: "update",
    possession: "any",
  })
  @common.Delete("/:id/requestData")
  async disconnectRequestData(
    @common.Param() params: EnumRequestDatumWhereUniqueInput,
    @common.Body() body: RequestDatumWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      requestData: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
