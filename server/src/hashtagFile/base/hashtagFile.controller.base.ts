/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { HashtagFileService } from "../hashtagFile.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { HashtagFileCreateInput } from "./HashtagFileCreateInput";
import { HashtagFileWhereInput } from "./HashtagFileWhereInput";
import { HashtagFileWhereUniqueInput } from "./HashtagFileWhereUniqueInput";
import { HashtagFileFindManyArgs } from "./HashtagFileFindManyArgs";
import { HashtagFileUpdateInput } from "./HashtagFileUpdateInput";
import { HashtagFile } from "./HashtagFile";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class HashtagFileControllerBase {
  constructor(
    protected readonly service: HashtagFileService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "HashtagFile",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: HashtagFile })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: HashtagFileCreateInput
  ): Promise<HashtagFile> {
    return await this.service.create({
      data: {
        ...data,

        fileId: data.fileId
          ? {
              connect: data.fileId,
            }
          : undefined,

        hashtagId: data.hashtagId
          ? {
              connect: data.hashtagId,
            }
          : undefined,
      },
      select: {
        createdAt: true,

        fileId: {
          select: {
            id: true,
          },
        },

        hashtagId: {
          select: {
            id: true,
          },
        },

        id: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "HashtagFile",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [HashtagFile] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(HashtagFileFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<HashtagFile[]> {
    const args = plainToClass(HashtagFileFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        createdAt: true,

        fileId: {
          select: {
            id: true,
          },
        },

        hashtagId: {
          select: {
            id: true,
          },
        },

        id: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "HashtagFile",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: HashtagFile })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: HashtagFileWhereUniqueInput
  ): Promise<HashtagFile | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,

        fileId: {
          select: {
            id: true,
          },
        },

        hashtagId: {
          select: {
            id: true,
          },
        },

        id: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "HashtagFile",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: HashtagFile })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: HashtagFileWhereUniqueInput,
    @common.Body() data: HashtagFileUpdateInput
  ): Promise<HashtagFile | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          fileId: data.fileId
            ? {
                connect: data.fileId,
              }
            : undefined,

          hashtagId: data.hashtagId
            ? {
                connect: data.hashtagId,
              }
            : undefined,
        },
        select: {
          createdAt: true,

          fileId: {
            select: {
              id: true,
            },
          },

          hashtagId: {
            select: {
              id: true,
            },
          },

          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "HashtagFile",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: HashtagFile })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: HashtagFileWhereUniqueInput
  ): Promise<HashtagFile | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,

          fileId: {
            select: {
              id: true,
            },
          },

          hashtagId: {
            select: {
              id: true,
            },
          },

          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
